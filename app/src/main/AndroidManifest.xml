<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- İnternet izni -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Konum izinleri -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Bildirim izni (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Pil optimizasyonu -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- WorkManager ve Alarm için -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Ezan sesi için -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <!-- Tam zamanlı alarm izinleri (Android 12+) -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RisaleEzanVaktiCompose"
        tools:targetApi="31">

        <!-- Ana Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.RisaleEzanVaktiCompose">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Namaz vakti alarm receiver'ı -->
        <receiver
            android:name=".service.PrayerTimeAlarmReceiver"
            android:enabled="true"
            android:exported="false" />

        <!-- Sesi durdur receiver'ı -->
        <receiver
            android:name=".service.StopSoundReceiver"
            android:enabled="true"
            android:exported="false" />

        <!-- Bildirimi kapat receiver'ı -->
        <receiver
            android:name=".service.DismissNotificationReceiver"
            android:enabled="true"
            android:exported="false" />

        <!-- Uygulamayı aç ve sesi durdur receiver'ı -->
        <receiver
            android:name=".service.OpenAppAndStopSoundReceiver"
            android:enabled="true"
            android:exported="false" />

        <!-- Vecizeyi paylaş receiver'ı -->
        <receiver
            android:name=".service.ShareVecizeReceiver"
            android:enabled="true"
            android:exported="false" />

        <!-- Cihaz açılışında alarm kurma -->
        <receiver
            android:name=".service.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Saat/tarih değişikliğinde alarm yenileme -->
        <receiver
            android:name=".service.TimeChangedReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
                <action android:name="android.intent.action.DATE_CHANGED" />
            </intent-filter>
        </receiver>

        <!-- Gece yarısı alarmı -->
        <receiver
            android:name=".service.MidnightAlarmReceiver"
            android:enabled="true"
            android:exported="false" />

        <!-- WorkManager initialization -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

    </application>

</manifest>