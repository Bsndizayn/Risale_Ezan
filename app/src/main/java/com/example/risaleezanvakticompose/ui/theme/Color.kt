package com.example.risaleezanvakticompose.ui.theme

import androidx.compose.ui.graphics.Color

// ==================== AÇIK TEMA RENKLERİ ====================

// Arka Plan Renkleri
val CreamBackground = Color(0xFFFFFBF5)       // Çok açık krem (pattern üzerine)
val WarmWhite = Color(0xFFFFFFFF)             // Beyaz kartlar
val LightCream = Color(0xFFFFF8E7)            // Açık krem surface

// Kırmızı Ana Renkler
val IslamicRed = Color(0xFFC62828)            // Ana kırmızı (Material Red 700)
val DeepRed = Color(0xFFB71C1C)               // Koyu kırmızı (Red 900)
val BrightRed = Color(0xFFD32F2F)             // Parlak kırmızı (Red 600)
val SoftRed = Color(0xFFEF5350)               // Yumuşak kırmızı (Red 400)
val LightRed = Color(0xFFFFEBEE)              // Çok açık kırmızı (Red 50)
val MediumRed = Color(0xFFFFCDD2)             // Orta açık kırmızı (Red 100)

// Altın Vurgu Renkleri
val GoldAccent = Color(0xFFD4AF37)            // Klasik altın
val GoldLight = Color(0xFFFFD700)             // Parlak altın
val GoldSoft = Color(0xFFE8C87E)              // Yumuşak altın

// Metin Renkleri (Açık Tema)
val TextDark = Color(0xFF212121)              // Koyu metin (primary)
val TextMedium = Color(0xFF424242)            // Orta metin (secondary)
val TextLight = Color(0xFF757575)             // Açık metin (tertiary)
val TextOnRed = Color(0xFFFFFFFF)             // Kırmızı üzerinde beyaz

// Divider ve Outline
val DividerLight = Color(0xFFE0E0E0)          // Açık ayırıcı
val OutlineLight = Color(0xFFBDBDBD)          // Açık outline

// Arapça Metin Özel Renkleri
val ArabicText = Color(0xFF000000)            // Siyah Arapça
val ArabicTextRed = Color(0xFFB71C1C)         // Kırmızı vurgu
val ArabicTextGold = Color(0xFFD4AF37)        // Altın vurgu

// ==================== KOYU TEMA RENKLERİ ====================

// Arka Plan Renkleri (Dark)
val DarkBackground = Color(0xFF121212)        // Ana koyu arka plan
val DarkSurface = Color(0xFF1E1E1E)           // Koyu yüzey
val DarkSurfaceVariant = Color(0xFF2C2C2C)    // Orta koyu yüzey

// Kırmızı Renkler (Dark Mode)
val LightRedDark = Color(0xFFEF5350)          // Açık kırmızı (dark için)
val BrightRedDark = Color(0xFFE57373)         // Parlak kırmızı (dark için)
val SoftRedDark = Color(0xFFFFCDD2)           // Yumuşak kırmızı container

// Altın (Dark Mode)
val GoldDark = Color(0xFFFFD700)              // Parlak altın
val GoldSoftDark = Color(0xFFFFE082)          // Yumuşak altın

// Metin Renkleri (Koyu Tema)
val TextDarkMode = Color(0xFFFFFFFF)          // Beyaz metin
val TextDarkSecondary = Color(0xFFE0E0E0)     // Açık gri metin
val TextDarkTertiary = Color(0xFFBDBDBD)      // Orta gri metin

// Divider (Dark)
val DividerDark = Color(0xFF424242)           // Koyu ayırıcı

// Özel Renkler
val CompletedGreen = Color(0xFF4CAF50)        // Tamamlanmış yeşil
val CompletedGreenLight = Color(0xFFC8E6C9)   // Açık yeşil container
val ErrorRed = Color(0xFFEF5350)              // Hata kırmızısı
val WarningOrange = Color(0xFFFF9800)         // Uyarı turuncu