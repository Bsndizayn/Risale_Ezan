package com.example.risaleezanvakticompose.ui.theme

import androidx.compose.ui.graphics.Color

// Açık Tema Renkleri
val CreamBackground = Color(0xFFF5F5F5)       // Açık gri
val BeigeLight = Color(0xFFFAFAFA)            // Çok açık gri
val WarmWhite = Color(0xFFFFFFFF)             // Tam beyaz

// Bordo Tonları - Açık Tema
val BurgundyPrimary = Color(0xFF9C1B3B)       // Canlı bordo
val DeepBurgundy = Color(0xFF7A1530)          // Koyu bordo
val SoftBurgundy = Color(0xFFB8314F)          // Yumuşak bordo

// Koyu Tema Renkleri - Daha Yumuşak ve Dengeli
val DarkBackground = Color(0xFF000000)        // Tam siyah
val DarkSurface = Color(0xFF121212)           // Material dark
val DarkSurfaceVariant = Color(0xFF1E1E1E)    // Orta koyu

// Koyu Tema için Bordo - Daha Soft
val LightBurgundy = Color(0xFFEF5350)         // Açık kırmızı
val SoftRose = Color(0xFFF48FB1)              // Soft pembe
val MutedBurgundy = Color(0xFFE57373)         // Açık bordo

// Text Renkleri - Açık Tema (Koyu text, beyaz yüzeyde)
val TextDark = Color(0xFF000000)              // Tam siyah
val TextMedium = Color(0xFF424242)            // Koyu gri
val TextLight = Color(0xFF757575)             // Orta gri

// Koyu Tema Text Renkleri (Açık text, koyu yüzeyde)
val TextDarkMode = Color(0xFFFFFFFF)          // Tam beyaz
val TextDarkSecondary = Color(0xFFE0E0E0)     // Açık gri
val TextDarkTertiary = Color(0xFFB0B0B0)      // Orta açık gri

// Divider ve Outline
val DividerColor = Color(0xFFBDBDBD)          // Açık tema - daha koyu
val DividerDark = Color(0xFF424242)           // Koyu tema - daha açık

// Aksan Renkleri
val GoldAccent = Color(0xFFFFA726)            // Modern turuncu-altın
val GoldLight = Color(0xFFFFB74D)             // Açık turuncu
val WhiteSmoke = Color(0xFFFAFAFA)            // Hafif gri beyaz